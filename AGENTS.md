# 项目协作规则

## 产品范围与验证

- 当前产品只面向桌面浏览器扩展，暂不考虑移动端。
- 除非用户后续明确要求，否则不要开展移动端适配、移动设备模拟或窄窗口／窄屏测试，也不要将这些检查列为默认验收步骤。
- UI 验证聚焦桌面浏览器扩展的实际使用场景，使用正常桌面窗口；不要为覆盖响应式断点而专门缩窄窗口。

## Git 提交

- 每次完成对项目文件或代码的修改后，都必须创建一次本地 Git commit，不要把已经完成的修改长期留在工作区。
- 除非用户在当前请求中明确要求，否则禁止执行远程 `push`。
- 提交信息遵循 Conventional Commits，格式为 `<type>: <中文说明>`。
- `type` 使用常见英文类型，例如 `feat`、`fix`、`refactor`、`docs`、`test`、`chore`。
- 冒号后的主题和必要的正文说明使用中文，准确描述本次改动。
- 一个提交只包含同一目的的相关修改；提交前运行与改动相称的检查。
- 当一次用户请求包含多个彼此独立的问题或功能时，必须按问题或功能拆分成多个小型 commit；不得等全部完成后合并成一个大提交。
- 每个拆分提交都必须能够独立说明其目的，并包含该部分对应的代码和测试；仅当该部分属于下述问题日志记录范围时，才同时更新问题日志。

## 问题日志

- 项目的独立问题日志位于 `docs/ISSUES.md`。
- 问题日志只记录已经确认的 Bug、异常、严重功能缺失，或对现有行为造成明确影响的设计与实现不足。
- Git 客户端、账户认证、权限、沙箱、网络和其他本机开发环境问题不属于项目问题；除非用户明确要求，否则不要写入 `ISSUE_LOG.md`，也不要仅为这类问题创建仓库提交。
- 新功能、新需求、普通体验优化、重构、测试补充和一般文档更新，不因发生项目修改而自动创建问题记录；除非该改动同时修复了符合上一条定义的具体问题。
- 不得为了满足提交形式而把普通需求包装成“问题”，也不得虚构现象、原因或影响。边界不清时默认不创建 Issue，可在提交信息或普通设计文档中说明改动背景。
- 日志至少记录：问题编号、日期、状态、现象或修改背景、原因分析、解决方案、验证方式和相关文件。
- 已定位但尚未修复的问题标记为“待修复”；完成修改和验证后，在同一条记录中改为“已解决”。
- 修改若用于处理已有或新确认的问题，代码与对应的 `docs/ISSUES.md` 更新必须放入同一个 commit，方便从提交记录追溯问题原因和处理过程。
- 规则生效前已经存在的历史问题记录保留，不要求回删或重新分类。
